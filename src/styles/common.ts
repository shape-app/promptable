export const enum TabIndex {
  Normal = 0,
  Important = 1,
}

export const enum AriaLabel {
  Button = 'button',
  Dialogue = 'dialogue',
}

export const enum FocusStyle {
  SelectableItem = 'outline-none',
}
